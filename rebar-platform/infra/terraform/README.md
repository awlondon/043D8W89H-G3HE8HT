# Terraform Modules

- `network/`: VPC, subnets, security groups.
- `db/`: RDS PostgreSQL configuration.
- `compute/`: ECS/EKS or EC2 launch templates for container workloads.
- `secrets/`: SSM Parameter Store or Secrets Manager plumbing.
- `storage/`: S3 buckets for job sheet images and call transcripts.
