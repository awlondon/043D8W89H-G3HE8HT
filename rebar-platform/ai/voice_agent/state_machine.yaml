states:
  PRE_CALL_CONTEXT:
    description: Gather CRM + lead metadata before dialing.
    next: CALL_START
  CALL_START:
    description: Connect call and deliver friendly opener.
    next: GATEKEEPER
  GATEKEEPER:
    description: Identify if gatekeeper or decision maker is present.
    next: INTRO_DECISION_MAKER
  INTRO_DECISION_MAKER:
    description: Brief intro and relevance statement for bending/fab shops.
    next: QUICK_QUALIFY
  QUICK_QUALIFY:
    description: Collect shop workflow type, crew size, and scrap pain level.
    next: VALUE_PITCH
  VALUE_PITCH:
    description: Offer key benefits tailored to pain points.
    next: QUESTIONS_OBJECTIONS
  QUESTIONS_OBJECTIONS:
    description: Address objections using knowledge base.
    next: CONTACT_CAPTURE
  CONTACT_CAPTURE:
    description: Confirm best contact details and follow-up preferences.
    next: WRAP_UP
  WRAP_UP:
    description: Schedule next step and set disposition.
    next: POST_CALL_SUMMARY
  POST_CALL_SUMMARY:
    description: Summarize transcript, key points, objections, and notes for humans.
