# Web Dashboard

Next.js dashboard used by operations and leadership to monitor project status, scrap analytics, and shop performance.

## Development

From the repo root:

```bash
pnpm --filter web-dashboard dev
```

The dev server runs on port 3001 by default (configurable via Next.js env vars).
