# Voice Sales Agent API

NestJS wrapper that exposes outbound calling workflows and webhooks for the AI voice agent. It will integrate with telephony providers (e.g., Twilio), orchestrate campaign state, and persist call metadata to the core API.
